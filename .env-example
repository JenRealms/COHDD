# COHDD Environment Variables Configuration
# Copy this file to .env and fill in your actual values
# Never commit the actual .env file to version control

# =============================================================================
# AWS Configuration
# =============================================================================

# AWS Access Key ID for accessing AWS services (S3, Athena, etc.)
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here

# AWS Secret Access Key for authentication
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# AWS Default Region (e.g., us-east-1, us-west-2, eu-west-1)
AWS_DEFAULT_REGION=us-east-1

# AWS Model ID for LLM services (e.g., anthropic.claude-3-sonnet-20240229-v1:0)
AWS_Model_ID=your_aws_model_id_here

# =============================================================================
# External API Keys
# =============================================================================

# OpenWeather API Key for weather data retrieval
# Get your free API key at: https://openweathermap.org/api
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Tavily API Key for web search functionality
# Get your API key at: https://tavily.com/
TAVILY_API_KEY=your_tavily_api_key_here

# =============================================================================
# Database Configuration (if using external databases)
# =============================================================================

# Database connection string (if applicable)
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# =============================================================================
# Application Configuration
# =============================================================================

# Application environment (development, staging, production)
ENVIRONMENT=development

# Debug mode (true/false)
DEBUG=true

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# =============================================================================
# Optional: Additional AWS Services
# =============================================================================

# S3 bucket for data storage (if different from default)
# S3_BUCKET_NAME=your-s3-bucket-name

# Athena workgroup (if using custom workgroup)
# ATHENA_WORKGROUP=your-athena-workgroup

# =============================================================================
# Optional: Monitoring and Analytics
# =============================================================================

# LangSmith API Key for LLM monitoring (optional)
# LANGSMITH_API_KEY=your_langsmith_api_key_here

# LangSmith Project Name (optional)
# LANGSMITH_PROJECT=cohdd-project

# =============================================================================
# Notes:
# =============================================================================
# 1. Replace all placeholder values with your actual credentials
# 2. Keep your .env file secure and never commit it to version control
# 3. Use IAM roles when possible instead of access keys in production
# 4. Regularly rotate your API keys and access credentials
# 5. Consider using AWS Secrets Manager or similar services for production deployments
